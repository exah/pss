<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

### Table of Contents

-   [Systems][1]
    -   [space][2]
        -   [Parameters][3]
        -   [Examples][4]
    -   [sizes][5]
        -   [Parameters][6]
        -   [Examples][7]
    -   [colors][8]
        -   [Parameters][9]
        -   [Examples][10]
    -   [boxStyle][11]
        -   [Parameters][12]
        -   [Examples][13]
    -   [textStyle][14]
        -   [Parameters][15]
        -   [Examples][16]
    -   [fontVariant][17]
        -   [Parameters][18]
        -   [Examples][19]
-   [Styles][20]
    -   [border][21]
        -   [Parameters][22]
        -   [Examples][23]
    -   [boxContentAlignment][24]
        -   [Parameters][25]
        -   [Examples][26]
    -   [boxItemsAlignment][27]
        -   [Parameters][28]
        -   [Examples][29]
    -   [boxSelfAlignment][30]
        -   [Parameters][31]
        -   [Examples][32]
    -   [clearFix][33]
        -   [Parameters][34]
        -   [Examples][35]
    -   [cursor][36]
        -   [Parameters][37]
        -   [Examples][38]
    -   [direction][39]
        -   [Parameters][40]
        -   [Examples][41]
    -   [display][42]
        -   [Parameters][43]
        -   [Examples][44]
    -   [ellipsis][45]
        -   [Parameters][46]
        -   [Examples][47]
    -   [flex][48]
        -   [Examples][49]
    -   [flexBox][50]
        -   [Parameters][51]
        -   [Examples][52]
    -   [float][53]
        -   [Parameters][54]
        -   [Examples][55]
    -   [gap][56]
    -   [grid][57]
        -   [Parameters][58]
        -   [Examples][59]
    -   [gridItem][60]
        -   [Examples][61]
    -   [hideOn][62]
        -   [Parameters][63]
        -   [Examples][64]
    -   [opacity][65]
        -   [Parameters][66]
        -   [Examples][67]
    -   [order][68]
        -   [Examples][69]
    -   [outline][70]
        -   [Parameters][71]
        -   [Examples][72]
    -   [position][73]
        -   [Parameters][74]
        -   [Examples][75]
    -   [radius][76]
        -   [Parameters][77]
        -   [Examples][78]
    -   [ratio][79]
        -   [Parameters][80]
        -   [Examples][81]
    -   [text][82]
        -   [Parameters][83]
        -   [Examples][84]
    -   [transform][85]
        -   [Parameters][86]
        -   [Examples][87]
    -   [transition][88]
        -   [Parameters][89]
        -   [Examples][90]
    -   [zIndex][91]
        -   [Parameters][92]
        -   [Examples][93]
-   [Atomic Styles][94]
    -   [atomic][95]
        -   [Parameters][96]
    -   [atomicDirection][97]
        -   [Parameters][98]
        -   [Examples][99]
    -   [atomicDisplay][100]
        -   [Parameters][101]
        -   [Examples][102]
    -   [atomicFlexItem][103]
        -   [Examples][104]
    -   [atomicPosition][105]
        -   [Parameters][106]
        -   [Examples][107]
    -   [atomicSizes][108]
        -   [Parameters][109]
        -   [Examples][110]
-   [Custom Styles][111]
    -   [createStyles][112]
        -   [Parameters][113]
        -   [Examples][114]
    -   [rule][115]
        -   [Parameters][116]
        -   [Examples][117]
    -   [boolValue][118]
        -   [Parameters][119]
        -   [Examples][120]
    -   [sizeValue][121]
        -   [Parameters][122]
        -   [Examples][123]
    -   [percentageValue][124]
        -   [Examples][125]
    -   [spaceValue][126]
        -   [Parameters][127]
        -   [Examples][128]
    -   [colorValue][129]
        -   [Parameters][130]
        -   [Examples][131]
    -   [themeValue][132]
        -   [Parameters][133]
        -   [Examples][134]
    -   [style][135]
        -   [Parameters][136]
        -   [Examples][137]
    -   [mediaStyle][138]
        -   [Parameters][139]
        -   [Examples][140]
    -   [themeStyle][141]
        -   [Parameters][142]
        -   [Examples][143]
    -   [createVariant][144]
        -   [Parameters][145]
        -   [Examples][146]
-   [Selectors][147]
    -   [propSelector][148]
        -   [Parameters][149]
        -   [Examples][150]
    -   [themeSelector][151]
        -   [Parameters][152]
        -   [Examples][153]
    -   [combineSelectors][154]
        -   [Parameters][155]
        -   [Examples][156]
-   [Mixins][157]
    -   [themePath][158]
        -   [Parameters][159]
        -   [Examples][160]
    -   [mq][161]
        -   [Parameters][162]
        -   [Examples][163]
    -   [prop][164]
        -   [Parameters][165]
        -   [Examples][166]
-   [Utils][167]
    -   [combineStyles][168]
        -   [Parameters][169]
        -   [Examples][170]
    -   [rem][171]
        -   [Parameters][172]
        -   [Examples][173]
    -   [px][174]
        -   [Parameters][175]
        -   [Examples][176]
    -   [splitUnit][177]
        -   [Parameters][178]
        -   [Examples][179]

## Systems




### space

```js
import { space } from 'pss'
```

Consistent `space` system for setting `margin`, or `padding`. Created with [spaceValue][126].

**Component props:**

-   `mg` → `margin`
-   `mgl` → `margin-left`
-   `mgr` → `margin-right`
-   `mgt` → `margin-top`
-   `mgb` → `margin-bottom`
-   `mgx` → `margin-left`, `margin-right`
-   `mgy` → `margin-top`, `margin-bottom`
-   `pd` → `padding`
-   `pdl` → `padding-left`
-   `pdr` → `padding-right`
-   `pdt` → `padding-top`
-   `pdb` → `padding-bottom`
-   `pdx` → `padding-left`, `padding-right`
-   `pdy` → `padding-top`, `padding-bottom`

**`Number` values:**

-   Value from `theme.space[mediaKey]`, `theme.space.all` or `theme.space` `Array` by index
-   Negative value for negative margins

**`Boolean` values:**

-   `true` value is equal to `1` index in space `Array`
-   `false` value is equal to `0` index in space `Array`

**`String` values:**

Treated same way as in [size][180].

-   Get value by path in `theme.size` or in top level `theme` object
-   If value in `theme.sizes` is an `Object` with media keys (like in `theme.media`) value is responsive
-   Other `String` values is passed as raw CSS value (like `'10%'` or `'100vh'`).

Related: [spaceValue][126], [sizes][5].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { space } from 'pss'

const Box = styled.div`
  ${space}
`
```

```js
const theme = {
  media: { sm: '(max-width: 600px)' },
  space: [ 0, 8, 16, 32, 65 ]
}

// `theme.space[1]`
<Box mg={1} /> // → margin: 8px;
<Box mgl={1} /> // → margin-left: 8px;
<Box mgt={true} /> // → margin-top: 8px;

// `theme.space[2]`
<Box mgy={2} /> // → margin-top: 16px; margin-bottom: 16px;
<Box mg={-2} /> // → margin: -16px;

// `theme.space[0]`
<Box mg={0} /> // → margin: 0;

// Responsive
<Box mgr={{ sm: -1 }} /> // → @media (max-width: 600px) { margin-right: -8px }
<Box mgr={{ all: 2, sm: -1 }} /> // → margin-right: 16px; @media (max-width: 600px) { margin-right: -8px }

// Custom values
<Box mgx='auto' /> // → margin-left: auto; margin-right: auto
<Box mgy='100px' /> // → margin-top: 100px; margin-bootom: 100px
```

```js
const theme = {
  media: {
    sm: '(max-width: 600px)'
  },
  space: {
    all: [ 0, 10, 20, 40, 80 ],
    sm: [ 0, 8, 16, 32, 64 ],
  }
}

// `theme.space.all[1]` and `theme.space.sm[1]`
<Box mg={1} /> // → margin: 10px; @media (max-width: 600px) { margin: 8px }
<Box mgl={1} /> // → margin-left: 10px; @media (max-width: 600px) { margin-left: 8px }
<Box mgt={true} /> // → margin-top: 10px; @media (max-width: 600px) { margin-top: 8px }

// `theme.space.all[2]` and `theme.space.sm[2]`
<Box mgy={2} /> // → margin-top: 20px; margin-bottom: 20px; @media (max-width: 600px) { margin-top: 16px; margin-bottom: 16px }
<Box mg={-2} /> // → margin: -20px; @media (max-width: 600px) { margin: -16px; }

// `theme.space.all[0]` and `theme.space.sm[0]`
<Box mg={0} /> // → margin: 0; @media (max-width: 600px) { margin: 0 }

// Responsive
<Box mgr={{ sm: -1 }} /> // → @media (max-width: 600px) { margin-right: -8px }
<Box mgr={{ all: 2, sm: -1 }} /> // → margin-right: 20px; @media (max-width: 600px) { margin-right: -8px }
```

### sizes

```js
import { sizes } from 'pss'
```

Consistent `sizes` system for `width`, `height`. Created with [sizeValue][121].

**Component props:**

-   `width` → `width`
-   `maxWidth` → `max-width`
-   `minWidth` → `min-width`
-   `height` → `height`
-   `maxHeight` → `max-height`
-   `minHeight` → `min-height`

**`String` values:**

-   Get value by path in `theme.size` or in top level `theme` object
-   If value in `theme.sizes` is an `Object` with media keys (like in `theme.media`) value is responsive
-   Other `String` values is passed as raw CSS value (like `'10%'` or `'100vh'`).

**`Number` values:**

-   From 0-1 it is converted to percentage widths
-   Greater than 1 are converted to pixel values.

**`Boolean` values:**

-   `true` is `100%`
-   `false` is `0`.

Related: [sizeValue][121], [space][2].

Examples use this `theme`:

```js
const theme = {
  media: {
    sm: `(max-width: 600px)`
  },
  size: {
    small: '10px',
    medium: '20px',
    card: {
      all: '500px',
      sm: '300px'
    }
  }
}
```

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { sizes } from 'pss'

const Box = styled.div`
  ${sizes}
`
```

```js
// `theme.size.small`
<Box height='small' /> // → height: 10px

// `theme.size.card.default` and `theme.size.card.sm`
<Box width='card' /> // → width: 500px; @media (max-width: 600px) { margin-left: 300px }

// only `theme.size.card.sm`
<Box width={{ sm: 'card' }} /> // → @media (max-width: 600px) { margin-left: 300px }

// Smaller that or equal to `1` is percentage value
<Box maxWidth={(1 / 2)} /> // → max-width: 50%

// `false` is `0`
<Box height={false} /> // → height: 0

// Convert to px
<Box minWidth={500} /> // → max-width: 500px
```

### colors

```js
import { colors } from 'pss'
```

Prop styles for getting current `palette` or `color` value from `theme`. Created with [colorValue][129].

Result can be changed in nested components with setting other key in `theme.default.palette`.

**Component props:**

-   `fg` → `color`
-   `bg` → `background-color`
-   `tm` → `color`, `background-color`

**`String` values:**

-   Valid color: `#f00`, `rgba(255, 255, 0, 0.2)`, `hsl(0, 100%, 50%)`
-   Key in `theme.color` or `theme.palette[theme.default.palette]`
    -   `black` → `theme.color.black`
    -   `accent` → `theme.palette.default.accent`

**`Boolean` values:**

-   `true` is different in each prop (takes specified value in `theme.palette[theme.default.palette]`)
    -   `bg` → `theme.palette.default.bg`
    -   `fg` → `theme.palette.default.fg`
    -   `tm` → `theme.palette.default.fg`, `theme.palette.default.bg`

Related: [colorValue][129], [rule][115].

Examples use this `theme`:

```js
const theme = {
  default: {
    palette: 'default' // this can be changed
  },
  color: {
    red: '#ff0000',
    black: '#222222',
    white: '#ffffff'
  },
  palette: {
    default: { // currently active
      bg: '#ffffff',
      fg: '#222222',
      accent: '#ff0000'
    },
    inverted: {
      bg: '#222222',
      fg: '#ffffff',
      accent: '#ff0000'
    }
  }
}
```

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { colors } from 'pss'

const Box = styled.div`
  ${colors}
`
```

```js
// theme.palette.default.bg
<Box bg /> // background-color: #ffffff

// theme.colors.black
<Box fg='black' /> // color: #222222

// theme.palette.default.accent
<Box fg='accent' /> // color: #ff0000

// theme.palette.default.bg, theme.palette.default.fg
<Box tm /> // background-color: #ffffff; color: #222222

// theme.palette.inverted.bg, theme.palette.inverted.fg
<Box tm='inverted' /> // background-color: #222222; color: #fffffff

// Valid color value
<Box bg="#ffff00" /> // background-color: #ffff00
```

### boxStyle

```js
import { boxStyle } from 'pss'
```

Global box styles system, like in [Sketch][182].

Related: [createVariant][144].

Add `boxStyle` to `theme`:

```js
const theme = {
  boxStyle: {
    red: {
      backgroundColor: 'red',
      color: 'white'
    },
    shadow: {
      boxShadow: '0 0 20px 0 rgba(0, 0, 0, .3)'
    }
  }
}
```

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { boxStyle } from 'pss'

const Box = styled.div`
  ${boxStyle}
`
```

```js
<Box boxStyle='red' /> // → background-color: red; color: white;
<Box boxStyle={[ 'red', 'shadow' ]} /> // → background-color: red; color: white; box-shadow: 0 0 20px 0 rgba(0, 0, 0, .3);
```

### textStyle

```js
import { textStyle } from 'pss'
```

Global text styles system, like in [Sketch][183].

Related: [createVariant][144], [typography][184].

Add `textStyle` to `theme`:

```js
const theme = {
  textStyle: {
    default: {
      fontSize: '16px',
      lineHeight: 1.2,
      fontWeight: normal,
      fontFamily: 'system-ui'
    },
    heading: {
      fontSize: '2rem',
      lineHeight: 1.2,
      fontWeight: 'bold',
      fontFamily: 'system-ui'
    }
  }
}
```

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { textStyle } from 'pss'

const Box = styled.div`
  ${textStyle}
`
```

```js
<Box textStyle={true} /> // → `theme.textStyle.default`
<Box textStyle='heading' /> // → `theme.textStyle.heading`
```

### fontVariant

```js
import { fontVariant } from 'pss'
```

Set `fontFamly` from `theme`:

```js
const theme = {
  default: {
    font: 'ui'
  },
  font: {
    heading: 'Times',
    ui: 'Helvetica'
  }
}
```

Related: [createVariant][144].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { fontVariant } from 'pss'

const Text = styled.p`
  ${fontVariant}
`
```

```js
<Text font={true} /> // → font-family: Helvetica
<Text font='ui' /> // → font-family: Helvetica
<Text font='heading' /> // → font-family: Times
<Text font='Comic Sans' /> // → font-family: Comic Sans
```

## Styles




### border

```js
import { border } from 'pss'
```

Set border with values from theme, created with [themeValue][132] and [colorValue][129].

| prop           | css             | type                | value | true                           | false |
| :------------- | :-------------- | :------------------ | :---- | :----------------------------- | :---- |
| `borderColor`  | `border-color`  | `String`, `Boolean` | ✓     | `theme.palette.default.border` | —     |
| `border`       | `border`        | `String`            | ✓     | `theme.border.default`         | —     |
| `borderLeft`   | `border-left`   | `String`            | ✓     | `theme.border.default`         | —     |
| `borderRight`  | `border-right`  | `String`            | ✓     | `theme.border.default`         | —     |
| `borderTop`    | `border-top`    | `String`            | ✓     | `theme.border.default`         | —     |
| `borderBottom` | `border-bottom` | `String`            | ✓     | `theme.border.default`         | —     |

Related: [colorValue][129], [themeValue][132], [rule][115],

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { border } from 'pss'

const Box = styled.div`
  ${border}
`
```

```js
// border-left: 1px dotted; border-color: red
<Box borderLeft='1px dotted' borderColor='red' />
```

### boxContentAlignment

```js
import { boxContentAlignment } from 'pss'
```

| prop             | css               | type     | value | true | false |
| :--------------- | :---------------- | :------- | :---- | :--- | :---- |
| `alignContent`   | `align-content`   | `String` | ✓     | —    | —     |
| `justifyContent` | `justify-content` | `String` | ✓     | —    | —     |

Related: [rule][115], [boolValue][118].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { boxContentAlignment } from 'pss'

const Grid = styled.div`
  display: grid;
  ${boxContentAlignment}
`
```

    <Grid alignContent='center'>
      <div>1</div>
      <div>2</div>
    </Grid>

### boxItemsAlignment

```js
import { boxItemsAlignment } from 'pss'
```

| prop           | css             | type     | value | true | false |
| :------------- | :-------------- | :------- | :---- | :--- | :---- |
| `alignItems`   | `align-items`   | `String` | ✓     | —    | —     |
| `justifyItems` | `justify-items` | `String` | ✓     | —    | —     |

Related: [rule][115]

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { boxItemsAlignment } from 'pss'

const Grid = styled.div`
  display: grid;
  ${boxItemsAlignment}
`
```

    <Grid alignItems='center'>
      <div>1</div>
      <div>2</div>
    </Grid>

### boxSelfAlignment

```js
import { boxSelfAlignment } from 'pss'
```

| prop          | css            | type     | value | true | false |
| :------------ | :------------- | :------- | :---- | :--- | :---- |
| `alignSelf`   | `align-self`   | `String` | ✓     | —    | —     |
| `justifySelf` | `justify-self` | `String` | ✓     | —    | —     |

Related: [rule][115]

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { boxSelfAlignment } from 'pss'

const Box = styled.div`
  ${boxSelfAlignment}
`
```

```js
<Grid>
  <Box alignSelf='center'>1</Box>
</Grid>
```

### clearFix

```js
import { clearFix } from 'pss'
```

| prop       | css                      | type   | value | true             | false |
| :--------- | :----------------------- | :----- | :---- | :--------------- | :---- |
| `clearFix` | [Clearfix][clearfix-url] | `true` | —     | Clear fix styles | —     |

Related: [float][53].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { clearFix } from 'pss'

const Box = styled.div`
  ${clearFix}
`
```

    <Box clearFix />

```js
&::after {
  content ' ';
  display: block;
  clear: both
}
```

### cursor

```js
import { cursor } from 'pss'
```

| prop     | css      | type     | value | true | false |
| :------- | :------- | :------- | :---- | :--- | :---- |
| `cursor` | `cursor` | `String` | ✓     | —    | —     |

⚠️ This prop may not be filtered by CSS-in-JS libraries, so you may need to provide custom prop filtering.

Related: [rule][115].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { cursor } from 'pss'

const Box = styled.div`
  ${cursor}
`
```

```js
<Box cursor='pointer' /> // → cursor: pointer
```

### direction

```js
import { direction } from 'pss'
```

| prop     | css      | type                          | value | true | false  |
| :------- | :------- | :---------------------------- | :---- | :--- | :----- |
| `left`   | `left`   | `String`, `Number`, `Boolean` | ✓     | `0`  | `auto` |
| `right`  | `right`  | `String`, `Number`, `Boolean` | ✓     | `0`  | `auto` |
| `top`    | `top`    | `String`, `Number`, `Boolean` | ✓     | `0`  | `auto` |
| `bottom` | `bottom` | `String`, `Number`, `Boolean` | ✓     | `0`  | `auto` |

Related: [position][73], [rule][115], [boolValue][118], [sizeValue][121].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { position, direction } from 'pss'

const Box = styled.div`
  position: relative;
  ${direction}
`
```

```js
<Box top={0.2} left={0} /> // position: relative; top: 20%; left: 0
```

### display

```js
import { display } from 'pss'
```

| prop      | css                      | type                | value | true      | false  |
| :-------- | :----------------------- | :------------------ | :---- | :-------- | :----- |
| `display` | [`display`][display-url] | `String`, `Boolean` | ✓     | `initial` | `none` |

[display-url]: https://developer.mozilla.org/en-US/docs/Web/CSS/display

Related: [hideOn][62], [boolValue][118], [rule][115].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { display } from 'pss'

const Box = styled.div`
  ${display}
`
```

```js
<Box display='inline-block' /> // display: inline-block;
```

### ellipsis

```js
import { ellipsis } from 'pss'
```

Info: [text-overflow][185].

Related: [boolValue][118], [rule][115].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { ellipsis } from 'pss'

const Text = styled.p`
  ${ellipsis}
`
```

```js
<Text ellipsis /> // → white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
```

### flex

```js
import { flex } from 'pss'
```

| prop   | css    | type     | value | true | false |
| :----- | :----- | :------- | :---- | :--- | :---- |
| `flex` | `flex` | `String` | ✓     | —    | —     |

⚠️ Some of this props may not be filtered by CSS-in-JS libraries (like `order`), so you may need to provide custom prop filtering.

Related: [rule][115], [boolValue][118], [sizeValue][121].

#### Examples

```js
import { flex } from 'pss'

const Box = styled.div`
  ${flex}
`
```

    <FlexBox> // display: flex
      <Box flex='1 1 auto'>2</Box> // flex: 1 1 auto
    </FlexBox>

### flexBox

```js
import { flexBox } from 'pss'
```

Styles for [Flexible Box Layout][186].

For alignment styles import [boxContentAlignment][24], [boxItemsAlignment][27].

| prop            | css              | type     | value | true | false |
| :-------------- | :--------------- | :------- | :---- | :--- | :---- |
| `flexWrap`      | `flex-wrap`      | `String` | ✓     | —    | —     |
| `flexDirection` | `flex-direction` | `String` | ✓     | —    | —     |

Related: [boxContentAlignment][24], [boxItemsAlignment][27], [rule][115], [boolValue][118].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { flexBox } from 'pss'

const FlexBox = styled.div`
  display: flex;
  ${flexBox}
`
```

    <FlexBox flexDirection='column' flexWrap={true}> // display: flex; flex-direction: column; flex-wrap: wrap;
      <div>1</div>
      <div>2</div>
    </FlexBox>

### float

```js
import { float } from 'pss'
```

| prop    | css                  | type     | value | true   | false  |
| :------ | :------------------- | :------- | :---- | :----- | :----- |
| `float` | [`float`][float-url] | `String` | ✓     | —      | —      |
| `clear` | [`clear`][clear-url] | `String` | ✓     | `both` | `none` |

[float-url]: https://developer.mozilla.org/en-US/docs/Web/CSS/float

[clear-url]: https://developer.mozilla.org/en-US/docs/Web/CSS/clear

Related: [rule][115], [boolValue][118].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { float } from 'pss'

const Box = styled.div`
  ${float}
`
```

```js
<Box float='left' /> // float: left
```

### gap

```js
import { gap } from 'pss'
```

Same as [space][2], but for [grid][57] and [flex][48].

**Component props:**

-   `gap` → `grid-gap`, `gap`
-   `columnGap` → `grid-column-gap`, `column-gap`
-   `rowGap` → `grid-row-gap`, `row-gap`

### grid

```js
import { grid } from 'pss'
```

Styles for [Grid Layout][187].

| prop                  | css                     | type     | value | true | false |
| :-------------------- | :---------------------- | :------- | :---- | :--- | :---- |
| `gridAutoFlow`        | `grid-auto-flow`        | `String` | ✓     | —    | —     |
| `gridAutoColumns`     | `grid-auto-columns`     | `String` | ✓     | —    | —     |
| `gridAutoRows`        | `grid-auto-rows`        | `String` | ✓     | —    | —     |
| `gridTemplateColumns` | `grid-template-columns` | `String` | ✓     | —    | —     |
| `gridTemplateRows`    | `grid-template-rows`    | `String` | ✓     | —    | —     |
| `gridTemplateAreas`   | `grid-template-areas`   | `String` | ✓     | —    | —     |

Related: [gap][56], [flex][48], [rule][115], [boolValue][118].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { grid } from 'pss'

const Grid = styled.div`
  display: grid;
  ${grid}
`
```

    <Grid gridTemplateColumns='repeat(12, 1fr)'> // display: grid; grid-template-columns: repeat(12, 1fr)
      <div>1</div>
      <div>2</div>
    </Grid>

### gridItem

```js
import { gridItem } from 'pss'
```

| prop         | css           | type     | value | true | false |
| :----------- | :------------ | :------- | :---- | :--- | :---- |
| `gridColumn` | `grid-column` | `String` | ✓     | —    | —     |
| `gridRow`    | `grid-row`    | `String` | ✓     | —    | —     |
| `gridArea`   | `grod-area`   | `String` | ✓     | —    | —     |

Related: [rule][115], [boolValue][118], [sizeValue][121].

#### Examples

```js
import { gridItem } from 'pss'

const GridItem = styled.div`
  ${gridItem}
`
```

    <Grid> // display: grid
      <GridItem gridRow='1' girdColumn='2'>1</GridItem>
      <GridItem gridRow='2' gridColumn='1'>2</GridItem>
    </Grid>

### hideOn

```js
import { hideOn } from 'pss'
```

| prop     | css             | type                 | value    | true | false |
| :------- | :-------------- | :------------------- | :------- | :--- | :---- |
| `hideOn` | `display: none` | key in `theme.media` | mediaKey | —    | —     |

Related: [display][42], [mediaStyle][138].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { hideOn } from 'pss'

const Box = styled.div`
  ${hideOn}
`
```

```js
<Box hideOn='sm' />
// @media (max-width: 600px) { display: none }

<Box hideOn={{ sm: true, md: true }} />
// @media (max-width: 600px) { display: none }
// @media (min-width: 601px) and (max-width: 1024px) { display: none }
```

### opacity

```js
import { opacity } from 'pss'
```

| prop      | css       | type               | value | true | false |
| :-------- | :-------- | :----------------- | :---- | :--- | :---- |
| `opacity` | `opacity` | `0...1`, `Boolean` | ✓     | `1`  | `0`   |

⚠️ This prop may not be filtered by CSS-in-JS libraries, so you may need to provide custom prop filtering.

Related: [rule][115], [boolValue][118].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { opacity } from 'pss'

const Box = styled.div`
  ${opacity}
`
```

```js
<Box opacity='1' /> // → opacity: 1
<Box opacity={0.5} /> // → opacity: 0.5
<Box opacity={true} /> // → opacity: 1
<Box opacity={false} /> // → opacity: 0
```

### order

```js
import { order } from 'pss'
```

| prop    | css     | type               | value | true | false |
| :------ | :------ | :----------------- | :---- | :--- | :---- |
| `order` | `order` | `Number`, `String` | ✓     | —    | —     |

Related: [rule][115], [boolValue][118], [sizeValue][121].

#### Examples

```js
import { order } from 'pss'

const Box = styled.div`
  ${order}
`
```

    <FlexBox> // display: flex
      <Box>2</Box>
      <Box order={1}>1</Box>
    </FlexBox>

### outline

```js
import { outline } from 'pss'
```

| prop      | css       | type                  | value | true | false |
| :-------- | :-------- | :-------------------- | :---- | :--- | :---- |
| `outline` | `outline` | `String`, `'debug'`\* | ✓     | —    | —     |

\* Random hex color, useful for debugging layout

⚠️ This prop may not be filtered by CSS-in-JS libraries, so you may need to provide custom prop filtering.

Related: [rule][115].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { outline } from 'pss'

const Box = styled.div`
  ${outline}
`
```

```js
<Box outline='1px solid red' /> // → outline: 1px solid red
```

### position

```js
import { position } from 'pss'
```

| prop       | css                        | type     | value | true | false |
| :--------- | :------------------------- | :------- | :---- | :--- | :---- |
| `position` | [`position`][position-url] | `String` | ✓     | —    | —     |

[position-url]: https://developer.mozilla.org/en-US/docs/Web/CSS/position

Related: [rule][115], [direction][39].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { position } from 'pss'

const Box = styled.div`
  ${position}
`
```

```js
<Box position='absolute' top={0.2} left={0} /> // position: absolute; top: 20%; left: 0
```

### radius

```js
import { radius } from 'pss'
```

| prop     | css             | type     | value | true | false |
| :------- | :-------------- | :------- | :---- | :--- | :---- |
| `radius` | `border-radius` | `Number` | ✓     | —    | —     |

⚠️ This prop may not be filtered by CSS-in-JS libraries, so you may need to provide custom prop filtering.

Related: [createVariant][144].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { radius } from 'pss'

const Box = styled.div`
  ${radius}
`
```

```js
<Box radius='5px' /> // → border-radius: 5px
<Box radius='round' /> // → `theme.radius.round` → border-radius: 9999px
```

### ratio

```js
import { ratio } from 'pss'
```

[Aspect Ratio Box][188] prop style
with pseudo elements.

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { ratio } from 'pss'

const Box = styled.div`
  ${ratio}
`
```

    <Box ratio={(16 / 9)} />

```css
.css::before {
  content: '';
  width: 1px;
  margin-left: -1px;
  float: left;
  height: 0;
  padding-bottom: 56.25%;
}

.css::after {
  content: '';
  display: table;
  clear: both;
}
```

### text

```js
import { text } from 'pss'
```

| prop            | css              | type               | value | true | false |
| :-------------- | :--------------- | :----------------- | :---- | :--- | :---- |
| `fontSize`      | `font-size`      | `String`, `Number` | ✓     | —    | —     |
| `fontWeight`    | `font-weight`    | `String`           | ✓     | —    | —     |
| `lineHeight`    | `line-height`    | `String`, `Number` | ✓     | —    | —     |
| `letterSpacing` | `letter-spacing` | `String`, `Number` | ✓     | —    | —     |
| `textAlign`     | `text-align`     | `String`           | ✓     | —    | —     |
| `whiteSpace`    | `white-space`    | `String`           | ✓     | —    | —     |

Related: [fontFamily][189], [ellipsis][45], [rule][115], [boolValue][118].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { text } from 'pss'

const Text = styled.p`
  ${text}
`
```

```js
<Text textAlign='center' /> // text-align: center
<Text lineHeight /> // line-height: normal
```

### transform

```js
import { transform } from 'pss'
```

| prop        | css         | type     | value | true | false |
| :---------- | :---------- | :------- | :---- | :--- | :---- |
| `transform` | `transform` | `String` | ✓     | —    | —     |

⚠️ This prop may not be filtered by CSS-in-JS libraries, so you may need to provide custom prop filtering.

Related: [rule][115].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { transform } from 'pss'

const Box = styled.div`
  ${transform}
`
```

```js
<Box transform='rotate(90deg)' /> // → transform: rotate(90deg)
```

### transition

```js
import { transition } from 'pss'
```

| prop         | css          | type     | value | true | false |
| :----------- | :----------- | :------- | :---- | :--- | :---- |
| `transition` | `transition` | `String` | ✓     | —    | —     |

Related: [rule][115].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { transition } from 'pss'

const Box = styled.div`
  ${transition}
`
```

```js
<Box transition='all .3s' /> // → transition: all .3s
```

### zIndex

```js
import { zIndex } from 'pss'
```

| prop     | css       | type                          | value | true | false  |
| :------- | :-------- | :---------------------------- | :---- | :--- | :----- |
| `zIndex` | `z-index` | `String`, `Number`, `Boolean` | ✓     | `1`  | `auto` |

Related: [position][73], [rule][115], [boolValue][118], [sizeValue][121].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { zIndex } from 'pss'

const Box = styled.div`
  position: relative;
  ${zIndex}
`
```

```js
<Box zIndex={10} /> // position: relative; z-index: 10;
```

## Atomic Styles




### atomic

```js
import { atomic } from 'pss'
```

Combination of

-   [space][2]
-   [colors][8]
-   [border][21]
-   [atomicSizes][108]
-   [atomicPosition][105]
-   [atomicDisplay][100]
-   [atomicFlexItem][103]

Related: [combineStyles][168].

#### Parameters

-   `props` **[Object][181]** 

### atomicDirection

```js
import { atomicDirection } from 'pss'
```

| prop | css             | type                          | value | true | false  |
| :--- | :-------------- | :---------------------------- | :---- | :--- | :----- |
| `l`  | `left`          | `String`, `Number`, `Boolean` | ✓     | `0`  | `auto` |
| `r`  | `right`         | `String`, `Number`, `Boolean` | ✓     | `0`  | `auto` |
| `t`  | `top`           | `String`, `Number`, `Boolean` | ✓     | `0`  | `auto` |
| `b`  | `bottom`        | `String`, `Number`, `Boolean` | ✓     | `0`  | `auto` |
| `x`  | `left`, `right` | `String`, `Number`, `Boolean` | ✓     | `0`  | `auto` |
| `y`  | `top`, `bottom` | `String`, `Number`, `Boolean` | ✓     | `0`  | `auto` |
| `z`  | `z-index`       | `String`, `Number`, `Boolean` | ✓     | `1`  | `auto` |

Related: [direction][39], [rule][115], [boolValue][118], [sizeValue][121].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { atomicDirection } from 'pss'

const Box = styled.div`
  position: absolute;
  ${atomicDirection}
`
```

```js
<Box t={0.2} l={0} /> // position: absolute; top: 20%; left: 0
<Box x y /> // position: absolute; top: 0; left: 0; right: 0; bottom: 0;
```

### atomicDisplay

```js
import { atomicDisplay } from 'pss'
```

| prop   | css                      | type                 | value    | true      | false  |
| :----- | :----------------------- | :------------------- | :------- | :-------- | :----- |
| `d`    | [`display`][display-url] | `String`, `Boolean`  | ✓        | `initial` | `none` |
| `hide` | `display: none`          | key in `theme.media` | mediaKey | —         | —      |

Related: [display][42], [boolValue][118], [rule][115], [mediaStyle][138].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { atomicDisplay } from 'pss'

const Box = styled.div`
  ${atomicDisplay}
`
```

```js
<Box d='inline-block' hide='sm' />
// display: inline-block; @media (max-width: 600px) { display: none }
```

### atomicFlexItem

```js
import { atomicFlexItem } from 'pss'
```

| prop | css          | type                | value | true    | false      |
| :--- | :----------- | :------------------ | :---- | :------ | :--------- |
| `f`  | `flex`       | `String`, `Boolean` | ✓     | `1 1 0` | `0 1 auto` |
| `o`  | `order`      | `Number`, `Boolean` | ✓     | `1`     | `0`        |
| `a`  | `align-self` | `String`            | ✓     | —       | —          |

Related: [flexItem][190], [rule][115], [boolValue][118], [sizeValue][121].

#### Examples

```js
import { atomicFlexItem } from 'pss'

const FlexBoxItem = styled.div`
  ${atomicFlexItem}
`
```

    <FlexBoxItem f='1 1'>2</FlexBoxItem> // flex: 1 1
    <FlexBoxItem o={-1}>1</FlexBoxItem> // order: -1

### atomicPosition

```js
import { atomicPosition } from 'pss'
```

| prop  | css        | type                            | value    | true       | false |
| :---- | :--------- | :------------------------------ | :------- | :--------- | :---- |
| `prl` | `position` | `Boolean`, key in `theme.media` | mediaKey | `relative` | —     |
| `pab` | `position` | `Boolean`, key in `theme.media` | mediaKey | `absolute` | —     |
| `pfx` | `position` | `Boolean`, key in `theme.media` | mediaKey | `fixed`    | —     |
| `psy` | `position` | `Boolean`, key in `theme.media` | mediaKey | `sticky`   | —     |
| `pst` | `position` | `Boolean`, key in `theme.media` | mediaKey | `static`   | —     |

Related: [position][73], [mediaStyle][138].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { atomicPosition } from 'pss'

const Box = styled.div`
  ${atomicPosition}
`
```

```js
<Box prl /> // position: relative;
<Box pab='sm' /> // @media (max-width: 600px) { position: absolute; }
```

### atomicSizes

```js
import { atomicSizes } from 'pss'
```

-   `w` → `width`
-   `maxw` → `max-width`
-   `minw` → `min-width`
-   `h` → `height`
-   `maxh` → `max-height`
-   `minh` → `min-height`

See [sizes][5] for docs.
Related: [sizeValue][121], [space][2].

#### Parameters

-   `props` **[Object][181]** 

#### Examples

```js
import { atomicSizes } from 'pss'

const Box = styled.div`
  ${atomicSizes}
`
```

```js
<Box w h /> // width: 100%; height: 100%;
```

## Custom Styles




### createStyles

```js
import pss from 'pss'
import { createStyles } from 'pss'
```

Create styles from [Object][191] with keys that represents component `prop` and
the value is a `style` that will be applied.

```js
{ [prop]: style | (input, props, mediaKey) => style }
```

-   `input` - prop value
-   `props` [Object][191] - component props, including `theme`
-   `mediaKey` [Object][191] - key in `theme.media`

In component prop accepts values:

-   [Boolean][192] — enable / disable default style value

    ```js
    const Comp = styled.div(createStyles({ red: { color: 'red' } }))

    <Comp red={true} /> // → color: red
    <Comp red={false} /> // → 🤷‍♂️
    ```

-   [String][193], [Number][194] or [Array][195] — handled in functional styles

    ```js
    const Comp = styled.div(createStyles({ width: (input) => ({ width: input } })))

    <Comp width='100px' /> // → width: 100px
    ```

-   [Object][191] with keys defined in `theme.media` to define values for different screen sizes

    ```js
    <Comp width={{ all: '100px', sm: '50px' }} /> // → width: 100px; @media (max-width: 600px) { width: 50px }
    ```

#### Parameters

-   `styles` **[Object][181]**  (optional, default `{}`)

#### Examples

```js
import { createStyles } from 'pss'

const styles = createStyles({
  display: value => ({ display: value }),
  hide: { display: 'none' },
  width: (value, props, mediaKey) => ({
    width: mediaKey === 'sm' && value === true ? '100%' : value
  })
})

const Box = styled.div`
  ${styles}
`

Box.propTypes = {
  ...styles.propTypes
}
```

```js
<Box display='inline-flex' /> // → display: inline-flex
<Box hide /> // → display: none
```

```js
// Add media queries
const theme = {
  media: {
    sm: '(max-width: 600px)'
  }
}

<Box theme={theme} width={{ all: '100px', sm: true }} /> // → width: 100px; @media (max-width: 600px) { width: 100% }

<ThemeProvider theme={theme}>
  <Box display='flex' hide={{ sm: true }} /> // → display: flex; @media (max-width: 600px) { display: none }
</ThemeProvider>
```

Returns **[Function][196]** `(props) => styles`

### rule

```js
import { rule } from 'pss'
```

Create style rule. Must be used with [createStyles][112].

#### Parameters

-   `cssProp` **[string][197]** 
-   `getValue`  
-   `value` **[Function][196]**  (optional, default `identity`)

#### Examples

```js
import pss, { rule } from 'pss'

const Box = styled.div(pss({
  display: rule('display')
}))
```

```js
// Add theme to ThemeProvider
<ThemeProvider theme={theme}>
  <Box display='flex' /> // → display: flex
</ThemeProvider>
```

Returns **[Function][196]** 

### boolValue

```js
import { boolValue } from 'pss'
```

Get value for rule based boolean condition, other values passed without modification.
Must be used with [rule][115].

#### Parameters

-   `trueValue`  
-   `falseValue`  
-   `defaultValue`  

#### Examples

```js
import pss, { rule, boolValue } from 'pss'

const Box = styled.div(pss({
  opacity: rule('opacity', boolValue(1, 0))
}))
```

```js
<Box opacity={true} /> // → opacity: 1
<Box opacity={false} /> // → opacity: 0
<Box opacity={0.5} /> // → opacity: 0.5
```

### sizeValue

```js
import { sizeValue } from 'pss'
```

Sizes system for any css prop. Default behaviour described in [sizes][5].
Must be used with [rule][115].

Related: [sizes][5], [rule][115], [spaceValue][126].

#### Parameters

-   `transformValue` **[Function][196]**  (optional, default `boolValue('100%',0)`)

#### Examples

```js
import pss, { rule, sizeValue, boolValue } from 'pss'

const sizes = pss({
  h: rule('height', sizeValue())
  w: rule('width', sizeValue()),
  l: rule('left', sizeValue(boolValue(0, 'auto'))),
  r: rule('right', sizeValue(boolValue(0, 'auto')))
})

const Box = styled.div`
  ${sizes}
`
```

```js
<Box w={0} /> // → width: 0
<Box h='300px' /> // → height: 300px
<Box l={{ all: 0, sm: 'auto' }} /> // → left: 0; @media (max-width: 600px) { left: auto }
<Box l={20} r={10} /> // → left: 20px; right: 10px
<Box l r /> // → left: 0; right: 0
```

Returns **[Function][196]** that must be used in [rule][115]

### percentageValue

```js
import { percentageValue } from 'pss'
```

Related: [sizes][5], [rule][115], [sizeValue][121].

#### Examples

```js
import pss, { rule, percentageValue } from 'pss'

const sizes = pss({
  w: rule('width', percentageValue())
})

const Box = styled.div`
  ${sizes}
`
```

```js
<Box w={1} /> // → width: 100%
<Box w={1 / 2} /> // → width: 50%
<Box w={0} /> // → width: 0
<Box w={100} /> // → width: 100px
```

Returns **[Function][196]** that must be used in [rule][115]

### spaceValue

```js
import { spaceValue } from 'pss'
```

Spacing system for `margin`, `padding`. Default behaviour described in [space][2].
Must be used with [rule][115].

Related: [space][2], [sizes][5], [rule][115], [sizeValue][121].

#### Parameters

-   `defaultValue`  — Fallback value used when prop value is [String][193] or nothing returned. (optional, default `sizeValue(identity)`)

#### Examples

```js
import pss, { rule, spaceValue } from 'pss'

const spaceRule = (name) => rule(name, spaceValue())

const margin = pss({
  mg: spaceRule('margin'),
  mgl: spaceRule('marginLeft'),
  mgr: spaceRule('marginRight'),
  mgt: spaceRule('marginTop'),
  mgb: spaceRule('marginBottom'),
  mgx: [ spaceRule('marginLeft'), spaceRule('marginRight') ],
  mgy: [ spaceRule('marginTop'), spaceRule('marginBottom') ]
})

const Box = styled.div`
  ${margin}
`
```

```js
const theme = {
  media: {
    sm: '(max-width: 600px)' // optional
  },
  space: [ 0, 8, 16, 32, 64 ]
}

<ThemeProvider theme={theme}>
  <Box mg={1} /> // → margin: 8px;
  <Box mgx={2} /> // → margin-left: 16px; margin-right: 16px
  <Box mg={{ sm: 1 }} /> // → @media (max-width: 600px) { margin: 8px }
</ThemeProvider>
```

```js
const theme = {
  media: {
    sm: '(max-width: 600px)'
  },
  space: {
    all: [ 0, 8, 16, 32, 64 ],
    sm: [ 0, 4, 8, 16, 32 ]
  }
}

<ThemeProvider theme={theme}>
  <Box mg={1} /> // → margin: 8px; @media (max-width: 600px) { margin: 4px }
  <Box mgx={2} /> // → margin-left: 16px; margin-right: 16px; @media (max-width: 600px) { margin-left: 8px; margin-right: 8px; }
  <Box mg={{ sm: 1 }} /> // → @media (max-width: 600px) { margin: 4px }
</ThemeProvider>
```

Returns **[Function][196]** that must be used in [rule][115]

### colorValue

```js
import { colorValue } from 'pss'
```

Get color from theme and apply it to css prop. Must be used with [rule][115].

#### Parameters

-   `key` **[string][197]** — Key in `theme.color` or in `theme.palette[theme.default.palette]`
-   `transformValue` **[Function][196]** — Return customized CSS prop value (i.e. `box-shadow`, gradients) (optional, default `identity`)

#### Examples

```js
import pss, { rule, colorValue } from 'pss'

const colors = pss({
  fg: rule('color', colorValue('fg')),
  bg: rule('backgroundColor', colorValue('bg')),
  shadow: rule('boxShadow', colorValue('shadow', (color) => `0 0 20px 0 ${color}`)),
  tm: [
     rule('color', colorValue('fg')),
     rule('backgroundColor', colorValue('bg'))
  ]
})

// Add to component
const Box = styled.div`
  ${colors}
`
```

```js
// theme.palette.default.fg
<Box fg={true} /> // background-color: #222222

// theme.colors.black
<Box fg='black' /> // color: #222222

// theme.palette.default.accent
<Box fg='accent' /> // color: #ff0000

// theme.palette.default.shadow
<Box shadow={true} /> // box-shadow: 0 0 20px 0 rgba(0, 0, 0, 0.2)

// theme.palette.default.fg, theme.palette.default.bg
<Box tm='default' /> // color: #222222; background-color: #ffffff
```

Returns **[Function][196]** 

### themeValue

```js
import { themeValue } from 'pss'
```

Use values defined in `theme[themeKey]`.

See [fontFamily][189], [radius][76].

#### Parameters

-   `options` **[Object][181]**  (optional, default `{}`)
    -   `options.themeKey` **[String][197]** 
    -   `options.transformValue` **[Function][196]**  (optional, default `identity`)
    -   `options.themeGetter` **[Function][196]**  (optional, default `getThemeValue(themeKey,transformValue)`)

#### Examples

```js
const theme = {
  font: {
    heading: 'Times New Roman, serif',
    ui: 'system-ui, Helvetica, sans-serif'
  }
}
```

```js
import pss, { themeValue } from 'pss'

const Text = styled.div(pss({
  fontFamily: rule('fontFamily', themeValue({ themeKey: 'font' }))
}))

<ThemeProvider theme={theme}>
  <Text fontFamily='ui'>
    Hello World!
  </Text>
</ThemeProvider>
```

```css
.css {
  font-family: system-ui, Helvetica, sans-serif;
}
```

Returns **[Function][196]** 

### style

```js
import { style } from 'pss'
```

Create style from value. Must be used with [createStyles][112].

Related: [rule][115].

#### Parameters

-   `options` **[Object][181]** 
    -   `options.cssProp`  
    -   `options.getStyle`   (optional, default `wrap(cssProp)`)
    -   `options.getValue`   (optional, default `identity`)

#### Examples

```js
import pss, { style, spaceValue } from 'pss'

const Box = styled.div(pss({
  gap: style({
    getStyle: (val) => ({ margin: val, padding: val }),
    getValue: spaceValue()
  })
}))
```

```js
// Add theme to ThemeProvider
<ThemeProvider theme={theme}>
  <Box gap={1} /> // → margin: 8px; padding: 8px;
</ThemeProvider>
```

### mediaStyle

```js
import { mediaStyle } from 'pss'
```

Create style wrapped in `theme.media`.

Related: [display][42].

#### Parameters

-   `style`  

#### Examples

```js
import pss, { mediaStyle } from 'pss'

const Box = styled.div(pss({
  hideOn: mediaStyle({ display: 'none' })
}))
```

```js
// Create theme with media queries
const theme = {
  media: {
    sm: '(max-width: 600px)'
  }
}

<ThemeProvider theme={theme}>
  <Box hideOn='sm' /> // @media (max-width: 600px) { display: 'none' }
</ThemeProvider>
```

### themeStyle

```js
import { themeStyle } from 'pss'
```

Use styles defined in `theme[themeKey]`. Useful for creating global shared styles.

Related: [textStyle][14].

#### Parameters

-   `options` **[Object][181]**  (optional, default `{}`)
    -   `options.themeKey` **[String][197]** 
    -   `options.transformValue` **[Function][196]**  (optional, default `identity`)
    -   `options.themeGetter` **[Function][196]**  (optional, default `getThemeValue(themeKey,transformValue)`)

#### Examples

```js
const theme = {
  textStyle: {
    default: {
      fontSize: '16px',
      lineHeight: 1.2,
      fontFamily: 'system-ui'
    },
    heading: {
      fontSize: '2rem',
      lineHeight: 1.2,
      fontWeight: 'bold',
      fontFamily: 'system-ui'
    }
  }
}
```

```js
import pss, { themeStyle } from 'pss'

const Text = styled.div(pss({
  textStyle: themeStyle({ themeKey: 'textStyle' })
}))

<ThemeProvider theme={theme}>
  <Text textStyle='heading'>
    Hello World!
  </Text>
</ThemeProvider>
```

```css
.css {
  font-size: 2rem;
  line-height: 1.2;
  font-weight: bold;
  font-family: system-ui;
}
```

Returns **[Function][196]** 

### createVariant

Create `variant` from styles defined directly in `theme`.
Inspired by [`styled-system`][198].

Related: [textStyle][14], [boxStyle][11], [rule][115], [themeValue][132], [themeStyle][141].

#### Parameters

-   `options` **[Object][181]** 
    -   `options.themeKey`  
    -   `options.prop`   (optional, default `'variant'`)
    -   `options.cssProp`   (optional, default `false`)
    -   `options.transformValue`  
    -   `options.themeGetter`   (optional, default `getThemeValue(themeKey)`)

#### Examples

```js
import { createVariant } from 'pss'

const Text = styled.p`
  ${createVariant({ themeKey: 'textStyle' })}
`
```

```js
const theme = {
  textStyle: {
    default: {
      fontSize: '16px',
      lineHeight: 1.2,
      fontWeight: normal,
      fontFamily: 'system-ui'
    },
    heading: {
      fontSize: '2rem',
      lineHeight: 1.2,
      fontWeight: 'bold',
      fontFamily: 'system-ui'
    }
  }
}

<Text variant='default' /> // → `theme.textStyle.default`
<Text variant='heading' /> // → `theme.textStyle.heading`
```

## Selectors




### propSelector

```js
import { ps } from 'pss'
```

Wrap result of prop style in custom CSS selector.

#### Parameters

-   `name` **[string][197]?** — CSS selector, like `&:first-child`, `& + &`
-   `value` **PropStyleValue?** — prop value

#### Examples

```js
import { space } from 'pss'

const Box = styled.div`
  ${space}
`
```

```js
import { ps } from 'pss'

<Box mgt={ps('& + &', 1)} />
```

```css
.css + .css { margin-top: 10px }
@media (max-width: 600px) { .css + .css { margin-top: 8px } }
```

### themeSelector

```js
import { ts, themePath } from 'pss'
```

Get value from `theme` directly in prop

#### Parameters

-   `fn`  

#### Examples

```js
const Box = styled.div`
  ${sizes}
`

<Box width={ts((theme) => theme.myValue)}
<Box width={ts(themePath('site.width'))}
```

### combineSelectors

```js
import { cs } from 'pss'
```

Combine any number of [propSelector][148]s.

#### Parameters

-   `selectors` **...any** 

#### Examples

```js
import { space } from 'pss'

const Box = styled.div`
  ${space}
`
```

```js
import { cs, ps } from 'pss'

<Box mgt={cs(2, ps('& + &', 1), ps('&:nth-of-type(2)', 0))} />
```

```css
.css { margin-top: 20px }
.css + .css { margin-top: 10px }
.css:nth-of-type(2) { margin-top: 0 }

@media (max-width: 600px) {
  .css { margin-top: 16px }
  .css + .css { margin-top: 8px }
}
```

## Mixins




### themePath

```js
import { themePath } from 'pss'
```

Get value from theme.

#### Parameters

-   `input`  
-   `defaultValue`  

#### Examples

```js
import { themePath } from 'pss'

const Box = styled.div`
  width: ${themePath('size.card')};
  background-color: ${themePath('color.red', 'hotpink')};
`

<Box /> // → width: 200px; background-color: hotpink;
```

### mq

```js
import { mq } from 'pss'
```

#### Parameters

-   `mediaKey`  — key in `theme.media`
-   `fallback`   (optional, default `'all'`)

#### Examples

```js
import { mq, themePath } from 'pss'

const Box = styled.div`
  @media ${mq('sm')} {
    background-color: ${themePath('color.red', 'hotpink')};
  }
`

<Box /> // → @media (max-width: 600px) { background-color: red; }
```

### prop

```js
import { prop } from 'pss'
```

#### Parameters

-   `propName`  
-   `fallback`  

#### Examples

```js
import { prop, themePath } from 'pss'

const Box = styled.div`
 background-color: ${prop('bg')};
 color: ${prop('color', themePath('color.primary'))};
 border-color: ${prop('borderColor', 'black')};
`

<Box bg='red' /> // → background-color: red; color: #0000FF; border-color: black;
<Box color='blue' /> // → color: blue; border-color: black;
```

## Utils




### combineStyles

Combine multiple styles together

#### Parameters

-   `fns` **...any** 

#### Examples

```js
import { combineStyles } from 'pss'
```

```js
import { combineStyles, margin, padding } from 'pss'

const space = combineStyles(
  margin,
  padding
)

const Space = styled.div`
  ${space}
`

Space.propTypes = {
  ...space.propTypes
}
```

### rem

Convert number to rem

#### Parameters

-   `input`   (optional, default `16`)
-   `base`   (optional, default `16`)

#### Examples

```js
import { rem } form 'pss'

rem() // → 1rem
rem(16) // → 1rem
rem(20) // → 1.25rem
rem('20px') // → 1.25rem
rem(20, 20) // → 1rem
```

### px

Convert numbers to pixels

#### Parameters

-   `num`  

#### Examples

```js
import { px } from 'pss'

px(30) // → '30px'
px(0) // → 0
px('100px') // → '100px'
px('100rem') // → '100rem' (nothing changed)
```

### splitUnit

#### Parameters

-   `input`  

#### Examples

```js
import { splitUnit } from 'pss'

const [ value, unit ] = splitUnit('30px') // → [ 30, 'px' ]
```

[1]: #systems

[2]: #space

[3]: #parameters

[4]: #examples

[5]: #sizes

[6]: #parameters-1

[7]: #examples-1

[8]: #colors

[9]: #parameters-2

[10]: #examples-2

[11]: #boxstyle

[12]: #parameters-3

[13]: #examples-3

[14]: #textstyle

[15]: #parameters-4

[16]: #examples-4

[17]: #fontvariant

[18]: #parameters-5

[19]: #examples-5

[20]: #styles

[21]: #border

[22]: #parameters-6

[23]: #examples-6

[24]: #boxcontentalignment

[25]: #parameters-7

[26]: #examples-7

[27]: #boxitemsalignment

[28]: #parameters-8

[29]: #examples-8

[30]: #boxselfalignment

[31]: #parameters-9

[32]: #examples-9

[33]: #clearfix

[34]: #parameters-10

[35]: #examples-10

[36]: #cursor

[37]: #parameters-11

[38]: #examples-11

[39]: #direction

[40]: #parameters-12

[41]: #examples-12

[42]: #display

[43]: #parameters-13

[44]: #examples-13

[45]: #ellipsis

[46]: #parameters-14

[47]: #examples-14

[48]: #flex

[49]: #examples-15

[50]: #flexbox

[51]: #parameters-15

[52]: #examples-16

[53]: #float

[54]: #parameters-16

[55]: #examples-17

[56]: #gap

[57]: #grid

[58]: #parameters-17

[59]: #examples-18

[60]: #griditem

[61]: #examples-19

[62]: #hideon

[63]: #parameters-18

[64]: #examples-20

[65]: #opacity

[66]: #parameters-19

[67]: #examples-21

[68]: #order

[69]: #examples-22

[70]: #outline

[71]: #parameters-20

[72]: #examples-23

[73]: #position

[74]: #parameters-21

[75]: #examples-24

[76]: #radius

[77]: #parameters-22

[78]: #examples-25

[79]: #ratio

[80]: #parameters-23

[81]: #examples-26

[82]: #text

[83]: #parameters-24

[84]: #examples-27

[85]: #transform

[86]: #parameters-25

[87]: #examples-28

[88]: #transition

[89]: #parameters-26

[90]: #examples-29

[91]: #zindex

[92]: #parameters-27

[93]: #examples-30

[94]: #atomic-styles

[95]: #atomic

[96]: #parameters-28

[97]: #atomicdirection

[98]: #parameters-29

[99]: #examples-31

[100]: #atomicdisplay

[101]: #parameters-30

[102]: #examples-32

[103]: #atomicflexitem

[104]: #examples-33

[105]: #atomicposition

[106]: #parameters-31

[107]: #examples-34

[108]: #atomicsizes

[109]: #parameters-32

[110]: #examples-35

[111]: #custom-styles

[112]: #createstyles

[113]: #parameters-33

[114]: #examples-36

[115]: #rule

[116]: #parameters-34

[117]: #examples-37

[118]: #boolvalue

[119]: #parameters-35

[120]: #examples-38

[121]: #sizevalue

[122]: #parameters-36

[123]: #examples-39

[124]: #percentagevalue

[125]: #examples-40

[126]: #spacevalue

[127]: #parameters-37

[128]: #examples-41

[129]: #colorvalue

[130]: #parameters-38

[131]: #examples-42

[132]: #themevalue

[133]: #parameters-39

[134]: #examples-43

[135]: #style

[136]: #parameters-40

[137]: #examples-44

[138]: #mediastyle

[139]: #parameters-41

[140]: #examples-45

[141]: #themestyle

[142]: #parameters-42

[143]: #examples-46

[144]: #createvariant

[145]: #parameters-43

[146]: #examples-47

[147]: #selectors

[148]: #propselector

[149]: #parameters-44

[150]: #examples-48

[151]: #themeselector

[152]: #parameters-45

[153]: #examples-49

[154]: #combineselectors

[155]: #parameters-46

[156]: #examples-50

[157]: #mixins

[158]: #themepath

[159]: #parameters-47

[160]: #examples-51

[161]: #mq

[162]: #parameters-48

[163]: #examples-52

[164]: #prop

[165]: #parameters-49

[166]: #examples-53

[167]: #utils

[168]: #combinestyles

[169]: #parameters-50

[170]: #examples-54

[171]: #rem

[172]: #parameters-51

[173]: #examples-55

[174]: #px

[175]: #parameters-52

[176]: #examples-56

[177]: #splitunit

[178]: #parameters-53

[179]: #examples-57

[180]: size

[181]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object

[182]: https://sketchapp.com/docs/styling/shared-styles/

[183]: https://sketchapp.com/docs/text/text-styles

[184]: typography

[185]: https://developer.mozilla.org/en-US/docs/Web/CSS/text-overflow

[186]: https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Flexible_Box_Layout/Basic_Concepts_of_Flexbox#The_flex_container

[187]: https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Grid_Layout

[188]: https://css-tricks.com/aspect-ratio-boxes/

[189]: fontFamily

[190]: flexItem

[191]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object

[192]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Boolean

[193]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/String

[194]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Number

[195]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Array

[196]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Statements/function

[197]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/String

[198]: https://github.com/jxnblk/styled-system
